const EventEmitter = require("events");

const myEmitter = new EventEmitter();

const def = myEmitter.on("name", (a, b, name) => {
  console.log(
    `My two value are ${a}, ${b} and there sum is ${a + b} also name= ${name}`
  );
});

myEmitter.emit("name", 1, 9, "hi");
def.emit("name", 6, 7, "hello");

///This is a event emitter allows us to track all the movement of user from server side like mouse click
/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
const EventEmitter = require("events");

const myEmitter = new EventEmitter();

setImmediate(() => {
  myEmitter.emit("123", 89);
});

myEmitter.on("123", () => {
  console.log("hello");
});

myEmitter.on("123", function () {
  console.log("motherfucker");
});

myEmitter.on("123", (a) => {
  console.log(`${a}`);
});

///setImmediate runs after all the the code in program gets executed

